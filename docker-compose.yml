version: '3.8'

services:
  qlearn:
    container_name: qlearn
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8001:8001"
    environment:
      - PORT=8001
      - DATABASE_URL=sqlite:///./quantum_edu.db
    restart: unless-stopped
